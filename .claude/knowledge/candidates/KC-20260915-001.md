---
# ── identity ────────────────────────────────────────────────
id: KC-20260915-001
status: promoted-knowledge
created: 2026-09-15
updated: 2026-09-15

# ── origin: which trigger produced this ─────────────────────
origin:
  type: discovery
  source: bhmiepy publication-readiness round — local CI-matrix corner verification
  location: "bhmiepy repo — build/cp312, conda envs bhmiepy + bhmiepy-np126"

# ── classification ──────────────────────────────────────────
topic: meson-editable-shared-build-dir
type: rule

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  Two conda environments with the SAME python tag editable-installing the
  same meson-python repository share and clobber the single
  build/<cp-tag>/ directory, so testing a second environment must either
  copy the repo (excluding build/) to a scratch location or re-run the
  main env's editable install afterwards to restore it.

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "Editable install from env bhmiepy-np126 (python 3.12) reconfigured and rebuilt the repo's build/cp312 that the main env bhmiepy (also 3.12) was importing from — meson-python keys the build dir by interpreter tag only"
  - "Recovery verified: re-running the main env's dev-install.cmd rebuilt build/cp312 with numpy 2.x and the fast suite went green again (74 passed)"
  - "Sibling practice in KC-20260914-004 (WSL validation copies the repo to /tmp excluding .git and build/) — that recipe now has a second, Windows-native reason"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: verified
  basis:
    - experiment

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: global
  potential: medium

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ─────────────────────────────────────
promotion:
  target: knowledge
  suggested_name: ""
  note: >
    Promoted in place (no knowledge base; single rule). Applies to any
    meson-python project tested from multiple same-tag environments
    (e.g. local CI-matrix verification: numpy 1.26 vs 2.x corners).
    Related: KC-20260914-004 (the /tmp-copy recipe), KC-20260913-001
    (the Windows editable-install chain being clobbered).

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: null

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: ""
outcome_reason: ""
---

## Notes

Plain `pip install .` (non-editable) does NOT touch the repo build
directory — pip builds in an ephemeral temp dir — so wheel-flow
verification against a second env is safe without precautions; only
editable installs collide.
