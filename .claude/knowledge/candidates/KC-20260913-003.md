---
id: KC-20260913-003
status: promoted-knowledge
created: 2026-09-13
updated: 2026-09-13

origin:
  type: discovery
  source: bhmiepy Phase 1 — running validation scripts in the bhmiepy conda env
  location: "conda 25.7.0 on Windows"

topic: conda-run-no-newline-args
type: fact

claim: >
  `conda run` refuses to execute commands whose arguments contain newlines
  (assertion "Support for scripts where arguments contain newlines not
  implemented"), so multiline `python -c` snippets must be written to a
  file and run as `conda run -n <env> python <file>` instead.

evidence:
  - "conda run -n bhmiepy python -c '<multiline>' → AssertionError in conda/cli/main_run.py wrap_subprocess_call: 'not any(\"\\n\" in arg for arg in arguments)'"
  - "the same script written to a temp .py file ran fine via conda run"
  - "conda error message itself suggests writing the script to an external file"

confidence:
  level: verified
  basis:
    - experiment
    - source-code

reuse:
  scope: global
  potential: medium

recurrence:
  occurrences: 1
  prior_ids: []

promotion:
  target: knowledge
  suggested_name: ""
  note: "Standalone promoted fact (R2: single fact, no skill). No knowledge base exists in this project; kept as promoted candidate record."

target_skill:
  existing: null

deferred_reason: ""
outcome_reason: ""
---

## Also considered and rejected this round (2026-09-13, T3 sweep)

- "for real m, qsca == qext only to ~5e-8 at small x (cancellation in the
  forward amplitude's real part)" — rejected as a knowledge candidate: the
  repo already records it (tests/test_mie.py test comment + DEV_PLAN §2.3);
  no durable value beyond the repository.
