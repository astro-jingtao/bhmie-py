---
# ── identity ────────────────────────────────────────────────
id: KC-20260915-002
status: candidate
created: 2026-09-15
updated: 2026-09-15

# ── origin: which trigger produced this ─────────────────────
origin:
  type: task-completion
  source: bhmiepy publication-readiness round — authoring .github/workflows/ci.yml (DEV_PLAN 5.2)
  location: "bhmiepy repo — .github/workflows/ci.yml"

# ── classification ──────────────────────────────────────────
topic: f2py-meson-fortran-ci-matrix
type: procedure

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  A f2py + meson-python Fortran extension repo is CI-tested on GitHub
  Actions with a {ubuntu, macos, windows} x Python x numpy matrix where
  POSIX jobs use system gfortran (apt/brew), Windows jobs use
  micromamba (conda-forge flang + flang-rt_win-64 + the python/numpy of
  the job) plus ilammy/msvc-dev-cmd with explicit CC=cl / FC=flang-new
  pins and the AR/RANLIB/LIB/FFLAGS tweaks of the local dev-install
  recipe, and meson.build forces -ffp-contract=off on gfortran/flang so
  near-identical translation units stay bit-identical on FMA-baseline
  archs (macOS arm64).

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "Workflow authored in bhmiepy .github/workflows/ci.yml (21 jobs after excluding py3.13+numpy1.26); YAML structure parse-verified"
  - "Windows build half mirrors the locally verified dev-install recipe (KC-20260913-001): same env vars, same conda packages"
  - "numpy-1.26 corner verified locally end-to-end (build + 74 tests, throwaway env)"
  - "FIRST LIVE RUN 2026-09-15 (run 34919186256, commit 2c2bc00): all 7 ubuntu jobs GREEN end-to-end — the POSIX half of the recipe is now verified, including apt gfortran and Linux slow tests"
  - "macOS 7/7: toolchain + fast suite green; only the slow bit-identity test failed via gfortran FMA contraction (see KC-20260915-005) — brew gcc as gfortran works"
  - "Windows numpy-1.26 3/3 failed at Build: meson preferred the runner image's MSYS2 gfortran over conda flang and died on flang-only FFLAGS ('Unable to detect linker for compiler gfortran -Wl,--version -static-libflangrt'); fixed by exporting CC=cl / FC=flang-new in the Build step (and mirrored in scripts/dev-install.cmd)"
  - "Windows numpy-2 4/4 failed at env creation: micromamba got the invalid spec numpy=>=2,<3 from YAML ternary composition (see KC-20260915-004)"
  - "setup-micromamba post-cleanup error 'Could not find micromamba activate <env> in .bash_profile' is a downstream symptom, not a cause: the action appends that line only after successful env creation (src/shell-init.ts), so its absence means the create step failed"
  - "Fixes verified locally (Windows/flang): rebuild with -ffp-contract=off accepted, 76 fast + 4 slow green; the re-run on GitHub Actions is the remaining open verification item"
  - "Follow-up same day: added an EXPERIMENTAL Windows mingw-gfortran corner job (include: windows/py3.12/numpy2/fortran=gfortran, continue-on-error) using the runner image's MSYS2 gcc/gfortran + plain pip deps + FFLAGS/LDFLAGS=-static-libgfortran -static-libgcc; the base matrix gained a single-value fortran:['flang'] dimension because an include whose keys all match an existing combo MERGES into it instead of creating a job (regression-tested in tests/test_ci_workflow.py). Required a meson.build fix: bhmie_ref's Windows stack flag is linker-syntax-dependent (/STACK for link.exe, --stack for mingw ld, via cc.get_argument_syntax()). Unverifiable locally (no mingw on the dev machine) — first signal will be its CI run"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: plausible
  basis:
    - experiment
    - source-code
  # POSIX half verified by a green live run; Windows half verified
  # locally only (CI re-run pending as of 2026-09-15)

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: global
  potential: high

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ─────────────────────────────────────
promotion:
  target: none-yet
  suggested_name: ""
  note: >
    Revisit trigger fired: first live run evidence recorded 2026-09-15
    with three root-caused failure modes and fixes. Promote (as an
    extension of the f2py-meson-flang-windows skill, CI section, via
    target_skill.existing) once a full post-fix run is green on all
    three OSes.

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: "f2py-meson-flang-windows (project)"

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: ""
outcome_reason: ""
---

## Notes

First-run risk list, resolved: micromamba activation under `bash -el`
on Windows worked (numpy-1.26 jobs reached the build step);
msvc-dev-cmd interplay fine; brew gcc provides `gfortran` on macOS.
The real first-run failures were elsewhere: MSYS2 gfortran on the
windows-latest image shadowing conda flang (CC/FC pins), the YAML
ternary spec bug (KC-20260915-004), and arm64 FMA contraction
(KC-20260915-005). Diagnostic path that worked: runs/jobs REST API
 anonymously for the failure map, job logs via the stored git
credential token (see KC-20260915-006).
