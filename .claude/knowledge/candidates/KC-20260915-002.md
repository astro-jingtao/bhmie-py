---
# ── identity ────────────────────────────────────────────────
id: KC-20260915-002
status: deferred
created: 2026-09-15
updated: 2026-09-15

# ── origin: which trigger produced this ─────────────────────
origin:
  type: task-completion
  source: bhmiepy publication-readiness round — authoring .github/workflows/ci.yml (DEV_PLAN 5.2)
  location: "bhmiepy repo — .github/workflows/ci.yml"

# ── classification ──────────────────────────────────────────
topic: f2py-meson-fortran-ci-matrix
type: procedure

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  A f2py + meson-python Fortran extension repo is CI-tested on GitHub
  Actions with a {ubuntu, macos, windows} x Python x numpy matrix where
  POSIX jobs use system gfortran (apt/brew) and Windows jobs use
  micromamba (conda-forge flang + flang-rt_win-64 + the python/numpy of
  the job) plus ilammy/msvc-dev-cmd, applying the same AR/RANLIB/LIB/
  FFLAGS tweaks as the local dev-install recipe.

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "Workflow authored in bhmiepy .github/workflows/ci.yml (21 jobs after excluding py3.13+numpy1.26); YAML structure parse-verified"
  - "Windows build half mirrors the locally verified dev-install recipe (KC-20260913-001): same env vars, same conda packages"
  - "numpy-1.26 corner verified locally end-to-end (build + 74 tests, throwaway env)"
  - "NOT YET RUN on GitHub Actions: the actions wiring (micromamba activation under bash -el on Windows, msvc-dev-cmd env interplay, brew gcc on macOS) is unverified as of 2026-09-15"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: plausible
  basis:
    - experiment
    - source-code
  # experiment covers the toolchain half only; action semantics from docs

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: global
  potential: high

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ─────────────────────────────────────
promotion:
  target: none-yet
  suggested_name: ""
  note: >
    Deferred until the first live CI run (revisit trigger: new
    evidence). If the matrix goes green (possibly after fixes), promote
    as an extension of the f2py-meson-flang-windows skill (CI section)
    or a sibling skill; route via target_skill.existing then. If it
    needed substantive changes, record the deltas as evidence first.

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: "f2py-meson-flang-windows (project)"

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: >
  The CI-actions wiring has not executed once; a procedure whose core
  steps are untested is a hypothesis, not verified know-how (R6).
  Revisit when the user pushes and the first run reports.
outcome_reason: ""
---

## Notes

Known risk points to check on first run: micromamba env activation in
`bash -el {0}` steps on Windows; ordering of msvc-dev-cmd (GITHUB_ENV)
vs conda PATH; `brew install gcc` symlink availability as `gfortran`;
conda-forge flang version drift (fallback: pin flang in create-args).
