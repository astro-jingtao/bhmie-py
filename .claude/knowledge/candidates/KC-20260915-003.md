---
# ── identity ────────────────────────────────────────────────
id: KC-20260915-003
status: deferred
created: 2026-09-15
updated: 2026-09-15

# ── origin: which trigger produced this ─────────────────────
origin:
  type: discovery
  source: bhmiepy publication-review — single-row table files rejected with a misleading error
  location: "bhmiepy src/bhmiepy/dust.py — Material.from_file / TableDistribution.from_file"

# ── classification ──────────────────────────────────────────
topic: numpy-loadtxt-single-row-ndmin
type: fact

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  np.loadtxt collapses a file with exactly one data row to a 1-D array,
  so column-count validation on the result misreports a valid one-row
  N-column file as malformed; pass ndmin=2 to keep a (1, N) table.

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "bhmiepy bug: a valid single-line 3-column refractive-index file hit 'must have three columns' because loadtxt returned shape (3,)"
  - "Fix (commit 47247fb): np.loadtxt(path, ndmin=2) in both from_file readers; regression tests test_from_file_single_row / test_table_from_file_single_row"
  - "numpy documents ndmin as 'The minimum number of dimensions of the returned array' — same parameter already used in bhmiepy's own load_outputs(ndmin=2)"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: verified
  basis:
    - experiment
    - official-documentation

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: global
  potential: low

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ─────────────────────────────────────
promotion:
  target: none-yet
  suggested_name: ""
  note: ""

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: null

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: >
  Single occurrence of documented numpy behavior; conservative default
  (prefer missing knowledge). Revisit on a second occurrence in this
  project's code review, then promote in place.
outcome_reason: ""
---
