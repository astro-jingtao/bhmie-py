---
# ── identity ────────────────────────────────────────────────
id: KC-20260914-002
status: promoted-knowledge
created: 2026-09-14
updated: 2026-09-14

# ── origin: which trigger produced this ─────────────────────
origin:
  type: discovery
  source: bhmiepy Phase 2 performance round, session 2026-09-14
  location: "src/bhmiepy/dust.py, src/bhmiepy/_fortran/bhmiepy_ext.f90 (bhmie_dust_accum)"

# ── classification ──────────────────────────────────────────
topic: f2py-extension-hot-loop-offload
type: rule

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  When a f2py extension's Python-side reduction over huge per-point output
  arrays dominates runtime, the fix is to move the reduction loop INTO the
  extension (intent(inout) accumulators, core results never crossing the
  boundary) -- NumPy-level batching (chunks/einsum/transposes) does not
  help, because the reduction is memory-bandwidth-bound, not
  dispatch-bound.

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "profile (cProfile + timed ext wrapper) on mrn77: loglog suspect cleared (~0.6% power / ~1.4% table workloads); split = core 64% / Python-NumPy accumulation 36%"
  - "NumPy chunking (24 bins/call + einsum 'k,kwa->wa' + transposes): machine-normalized lib/exe got WORSE (0.73 vs 0.55 before) -- bandwidth, not dispatch"
  - "bhmie_dust_accum (upstream caller-loop body ported into the extension, inout accumulators, s1/s2 never materialized in Python): +21% end-to-end machine-normalized, mrn77 1.80x->2.29x, custom 1.66x->2.09x; golden + live A/B all green"
  - "f2py quirk hit en route: zero-size input arrays fail with 'unexpected array size' -- guard empty batches Python-side (see KC-20260913-001)"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: verified
  basis:
    - experiment
    - repeated-observation

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: project
  potential: medium

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ──────────────────────────────────────
promotion:
  target: knowledge
  suggested_name: ""
  note: >
    Promoted in place (no knowledge base; single project so far). Applies
    verbatim to any future hot loop in this package (e.g. moving the
    weight_number table integrals, or multi-wavelength material batching).

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: null

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: ""
outcome_reason: ""
---
