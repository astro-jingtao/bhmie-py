---
# ── identity ────────────────────────────────────────────────
id: KC-20260915-006
status: candidate
created: 2026-09-15
updated: 2026-09-15

# ── origin: which trigger produced this ─────────────────────
origin:
  type: discovery
  source: bhmiepy CI first-run diagnosis — pulling failure details for a public repo without gh CLI
  location: "GitHub Actions REST API + git credential store"

# ── classification ──────────────────────────────────────────
topic: github-actions-log-access-without-gh
type: fact

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  For a public GitHub repository, the Actions runs/jobs REST endpoints
  (failed-step map, job IDs) are readable unauthenticated (per-IP rate
  limit), while raw job logs require auth (UI shows "Sign in to view
  logs"; the logs endpoint 403s anonymously) — and on a machine with a
  stored github.com git credential, `git credential fill` yields a
  token that fetches those logs read-only with an Authorization
  header.

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "GET /repos/astro-jingtao/bhmie-py/actions/runs/34919186256/jobs?per_page=50 via WebFetch (anonymous): full 21-job failed-step map with job IDs, e.g. 104223248854"
  - "GET .../actions/jobs/104223248854/logs anonymously: HTTP 403; the HTML job page shows 'Sign in to view logs' — logs are gated even on public repos"
  - "`git credential fill` (protocol=https, host=github.com) returned a usable token; Invoke-RestMethod with `Authorization: Bearer <token>` downloaded the full job log, which contained the root-cause lines (micromamba `numpy=>=2,<3`)"
  - "Caveat observed in the same session: the anonymous REST API from this machine's IP hit 'API rate limit exceeded' under Invoke-RestMethod but succeeded via WebFetch — retry through a different egress before concluding the API is blocked"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: verified
  basis:
    - experiment

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: global
  potential: medium

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ─────────────────────────────────────
promotion:
  target: none-yet
  suggested_name: ""
  note: >
    Single occurrence; keep as a standalone fact candidate. Reassess
    if CI debugging without gh recurs (2nd occurrence → fold into a
    small diagnostics section of whatever CI skill gets promoted).

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: null

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: ""
outcome_reason: ""
---

## Notes

Hygiene: never print the credential token into the transcript —
extract it into a variable inside one shell command and use it
directly. Read-only GETs of one's own repo logs only.
