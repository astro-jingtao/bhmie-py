---
# ── identity ────────────────────────────────────────────────
id: KC-20260913-004
status: promoted-knowledge
created: 2026-09-13
updated: 2026-09-13

# ── origin: which trigger produced this ─────────────────────
origin:
  type: user-correction
  source: bhmiepy README review, session 2026-09-13
  location: "scripts/dev-install.cmd, scripts/test.cmd, README.md Installation"

# ── classification ──────────────────────────────────────────
topic: machine-local-scripts-out-of-git
type: rule

# ── the claim: one falsifiable sentence ─────────────────────
claim: >
  Machine-local helper scripts (hardcoded toolchain/conda/IDE paths) must
  not be git-tracked: untrack + gitignore them (keep on disk for dev),
  purge them from history if ever committed, and keep public docs (README
  installation sections) generic — prerequisites + pip/pytest commands.

# ── evidence: what supports the claim ───────────────────────
evidence:
  - "user 2026-09-13 on bhmiepy README Installation: scripts are 本机专门的内容, 不适合放在这里也不适合 git track; 从 git 中删掉且把历史记录里的也删除（泄露本机配置）; 本机保留以便开发; README 应该用更 general 的说法"
  - "scripts/dev-install.cmd + scripts/test.cmd hardcoded C:\\Users\\<user>\\anaconda3 and the VS 2022 BuildTools vcvarsall path; were present in the scaffold and bench commits (pre-purge hashes 1635e94, bd865b4)"
  - "fix applied: git rm --cached scripts/*, /scripts/ added to .gitignore, CLAUDE.md notes the local-only rule, README Installation rewritten generic (toolchain prereqs + pip install -e .), history purged via git filter-branch --index-filter 'git rm -r --cached --ignore-unmatch scripts'"

# ── confidence ──────────────────────────────────────────────
confidence:
  level: user-confirmed
  basis:
    - user-confirmation

# ── reuse assessment ────────────────────────────────────────
reuse:
  scope: global
  potential: medium

# ── recurrence tracking ─────────────────────────────────────
recurrence:
  occurrences: 1
  prior_ids: []

# ── promotion decision ──────────────────────────────────────
promotion:
  target: knowledge
  suggested_name: ""
  note: >
    Promoted in place (no knowledge base exists; single rule does not merit
    one). Project-local enforcement lives in bhmiepy CLAUDE.md
    ("scripts/ is local-only and untracked"); this candidate additionally
    records the user-level rule: no machine-specific content in tracked
    files, git history, or public docs of any of their repos.

# ── skill routing (only when target: skill) ─────────────────
target_skill:
  existing: null

# ── defer/reject bookkeeping ────────────────────────────────
deferred_reason: ""
outcome_reason: ""
---

## Notes

- The git-purge procedure itself (git rm --cached → filter-branch → delete
  refs/original → reflog expire → gc --prune=now) is standard git know-how,
  deliberately not persisted as a separate candidate.
- Benchmark-coverage fact (only Phase 1 has benchmarks/bench_upstream.py;
  Phase 2 has none) is derivable from the repo — not persisted.
